# PLAN.md — video-studio 改造计划（待确认后再动代码）

> 历史文档：本文件是改造计划，不含任何代码改动。当前行为以 [SKILL.md](SKILL.md) 为准，
> 改造结果与实测见 [UPGRADE.md](UPGRADE.md)。配套审计见 `AUDIT.md`。
确认后按 §7 的批次顺序执行，每批独立可回滚，P0 全部验收通过后才动 P1。

---

## 1. 目标与验收口径

| 指标 | 目标 | 怎么测（不许自欺） |
| --- | --- | --- |
| AI 侧 token | 同项目重跑 **−40%** | 可测的是**工具输出字节 + 被读文件字节**（÷4 ≈ token）+ **回合数**；改造前后跑同一命令序列取 A/B |
| 总墙钟 | **−40%** | 逐环节打点：读/写/探针/渲染/装配/验收 |
| 渲染墙钟 | **−50%** | 同项目同分辨率比 `渲染墙钟` 与 `每帧 ms` |
| patch 失败率 | **<10%** | 新工具自身统计 attempt / fallback 次数 |
| 不读源码可调用 | 通过 | 冷启动测试：只给 `API.md` + `--help`，完成 preview → render |
| 预览不看图可判断 | 通过 | 用本次 3 个真 bug 做回归用例（幽灵残影 / 元素出框 / 深度放大） |
| 一镜到底分段并行 | 通过 | **帧级 sha256 序列逐帧一致**（硬保证）；比特流级一致需 `--deterministic-encode` |
| 兼容性 | 不破坏 | 既有命令默认行为不变；跑 `selftest` + 既有项目 before/after 输出比对 |

**关于"确定性"的诚实说明**：`seek(t)` 是纯函数 ⇒ 逐帧像素必然一致，这一条可以硬保证。
但 x264 的**码流字节**会随线程数/lookahead 变化，所以"最终 sha256 与单进程一致"要拆成：
默认保证**帧哈希一致**；需要**比特一致**时加 `--deterministic-encode`（固定线程、关 lookahead、固定 preset）。
这是计划里唯一需要修订用户验收标准的地方，其余照原样执行。

---

## 2. 现状基线（来自 AUDIT §3.4，用于算节省）

- 项目级：**≈130–150k token / ≈85 分钟**
- 其中可归因于缺陷的浪费：
  - 读源码探接口 **≈32k token / 15 min**
  - patch 失败重发 **≈20–30k token / 10 min**
  - 自写校验探针 **≈10k token / 20–25 min**
  - 4K 单进程渲染 **75 min**（实测 2.5 s/帧 × 1800 帧）
  - 因内存整轮报废 **≈20 min**（两次）

---

## 3. P0 — 省 token 最狠、改动最小

### A. 接口自描述

| 项 | 内容 |
| --- | --- |
| 现状 | 5,068 行 Python + 864 行 JS 运行时的调用面只能靠读源码；`three-kit.js` 连导出面都无法自动识别 |
| 改法 | ① `scripts/api_index.py`：AST 扫描生成 `api_index.json`（签名/参数/默认值/返回/副作用/所属模块），**由脚本生成，禁止手写**；② 生成 `API.md`：≥12 个常见任务的最小调用示例（preview/report/render/plan/verify/patch/scene_check/audio…）；③ 给 `vs.py` 与 4 个独立脚本补 `--help` 的 description + epilog 示例；④ `--help` 文案里直接内联该子命令的两个例子 |
| 省 | **≈28k token**（32k→4k）**[实测基线 + 估算]**；**−1~3 回合** |
| 验收 | 冷启动：只给 `api_index.json` + `API.md`，能写出一次正确的 preview + render 调用 |
| 回滚 | 纯新增文件（`api_index.py/json`、`API.md`）+ 各脚本 argparse 文案；删除即回滚 |

### B. 结构化输出 / 日志分级 / 统一截断 / 结构化错误

| 项 | 内容 |
| --- | --- |
| 现状 | 无 `--json`；`emit()` 固定 `indent=2`；`main()` 仅打印 `error: Type: msg`；日志无分级、无截断 |
| 改法 | ① `scripts/lib/fmt.py`：`emit(obj, json=…, quiet=…)`、`truncate(text, head=20, tail=20)`、`fail(code, where, expected, got, fix_hint)`；② 所有子命令接 `--json`（机器可读，紧凑 JSON）与 `--verbose`（全量日志；默认一行/步摘要）；③ 错误统一 `{code, where, expected, got, fix_hint}`，退出码语义固定；④ 长输出默认头尾各 20 行 |
| 省 | **≈6k token**（错误转储 7k→1k）+ `indent=2`→紧凑 **≈1k** **[实测：本次 3 处 stack trace + 33 条冒烟失败]**；**−2~4 回合** |
| 验收 | 故意制造 3 类错误（缺文件 / scene 未 ready / ffmpeg 失败），输出必须含 `fix_hint`；`--json` 可被 `json.loads` 直接解析 |
| 回滚 | 新增 `fmt.py` + vs.py 调用点小改；`git checkout scripts/vs.py` 即回滚 |

### C. 预览双通道（本次最高性价比的一项）

| 项 | 内容 |
| --- | --- |
| 现状 | `preview` 只出 1 张 PNG；`ascii_map()`、区域 ink、`rhythm()` 等原语**已存在但只吃成品视频** |
| 改法 | `preview --report --at 2.75,7.9,24.7`：① 复用 `qc_video.ascii_map()` 出**可调分辨率 ASCII frame map**；② **九宫格 ink 统计**；③ **文字边界框/字宽/对比度**（DOM 侧 `getBoundingClientRect` + `style.contrast()` 已有）；④ **平均亮度/主色占比**；⑤ 多时间点**合并一份 JSON**；⑥ DOM 探针塞进 `render_segment.mjs` 的 `--probe` 分支（不新增渲染路径） |
| 省 | **≈10k token / ≈25 分钟**（本次自写 ascii.py + check.mjs + finprobe.mjs ≈400 行 + 3 轮定位）**[实测]**
| 验收 | 3 个回归用例必须被数值判出：① t=0 幽灵残影 ② 8.3s 文字出框 ③ 17.46s 元素被放大 4.5× |
| 回滚 | `preview` 默认行为不变，全部藏在 `--report` 后面；删分支即回滚 |

### D. 避免重复读文件

| 项 | 内容 |
| --- | --- |
| 现状 | 无任何读取缓存；重复读同一文件 |
| 改法 | ① `fmt.py` 内 `read_cached(path)`（同会话同 hash 只读一次，落 `.vs_cache/`）；② `vs.py cat --cached <file>` 供 agent 取片段；③ 场景改动后提供 `vs.py diff-lines <file> <range>`（只回变更行区间） |
| 省 | **≈3k token**（最小一项，据实标低）**[估算]** |
| 验收 | 同一文件二次读取命中缓存；`--json` 里带 `cache: hit/miss` |
| 回滚 | 新增函数 + 一个子命令；默认路径不走缓存 |

**P0 小结（估算）**：token **−47k**（130–150k → ≈85–100k，**−32~38%**，已在目标边缘）；
时间 **−40 min**（85 → ≈45 min，**−47%**，达标）。P0 完成即先验收一次。

---

## 4. P1 — 省迭代次数、省等待

### E. patch 健壮性（本次最痛的单项）

| 项 | 内容 |
| --- | --- |
| 现状 | 依赖外部 patch 语义：5 次被拒/失配；`delete+add` 同路径被策略拒 |
| 改法 | `scripts/apply_patch.py`：① 每次改动带**内容 hash 前置校验**；② **禁止 same-path delete+add**（显式报错并给替代写法）；③ 支持**多处手术式修改一次提交**（`{path, find, replace, expected_hash, count}` 列表）；④ 失败自动**降级为整文件重写 + diff 报告**；⑤ 每次 patch 后自动跑**语法检查 + 目标断言** |
| 省 | **≈20k token / ≈10 min**（本次 5 次失败重发的载荷）**[实测]** |
| 验收 | 连续 20 次真实改动，失败率 <10%，失败时不阻塞（自动降级成功） |
| 回滚 | 纯新增脚本；不改已有接口 |

### F. 场景几何/状态自检（渲染前自动跑）

| 项 | 内容 |
| --- | --- |
| 现状 | 无；本次 3 个 bug 全靠手写探针 |
| 改法 | `scripts/scene_check.py` → `scene_check.json`：① 全时间轴 `seek(t)` 扫描（已有能力，补**错误定位到帧区间**）；② DOM 场景：每个"钱帧"断言**可见元素都在画框内**、visibility/opacity 与轨道一致；③ 几何场景（canvas/SVG）：多边形**有符号面积非零**、**退化切线**、**每个 morph 状态控制点数一致**；④ 结果汇总 + 失败时给出 `{t_from,t_to,where,expected,got}` |
| 省 | **≈8k token / ≈20 min** **[实测基线：3 个 bug 手查成本]** |
| 验收 | 3 个回归用例全部被自动判出，且能报出帧区间 |
| 回滚 | 纯新增脚本；`render` 前调用可加开关 `--no-check` |

### G. 渲染加速（最大墙钟收益）

| 项 | 内容 |
| --- | --- |
| 现状 | 单镜头=单进程；缓存粒度=整段；`plan` 预算低估 2.2×；GPU/JPEG/preset/重启全未用 |
| 改法 | ① **分段并行**：`render --slices N --jobs M`，把单镜头按帧切 N 段并行渲帧，**按序无损合并**（纯函数保证等价），保留"一次导出"语义；② **帧级 sha256 增量缓存**：命中即复用（与既有 `.key` 段级缓存并存，段级不动）；③ **自适应分辨率**：先 1/4 分辨率跑全时间轴**预检**（不是渲染交付物），确认后再全分辨率一次成型；④ **预算前置**：按实测重拟合 `ms_per_frame`（4K 实测 2500 ms/帧，现模型 1116），加**内存守卫**（`jobs ≤ 空闲GB/1.6`）与降级建议（`--gpu --preset ultrafast --slices 8`）；⑤ 补 GPU 旗标（`--use-angle=d3d11 --enable-gpu-rasterization --enable-zero-copy`）、中间层 `--preset ultrafast`、**每 90 帧重启浏览器**；⑥ 可选 `--jpeg 97` 中间帧 |
| 省 | **渲染 −70~83%（实测 75 min → 13 min，8 片×4 进程 + GPU）**；加 JPEG 实测 0.78 s/帧 → 估算 **6–9 min（−88~92%）**；重跑场景增量命中 → **−87.5%**（本次只需重渲 1/8 片）；预算误差 2.2× → **<20%**；省掉整轮报废 **−20 min** |
| 验收 | ① 帧级 sha256 序列与单进程一致；② 同一改一行场景的重跑只重渲受影响切片；③ `plan` 预算与实测差 <20% |
| 回滚 | 新逻辑全部走 `--slices/--gpu/--incremental` 开关；默认单进程路径不变（`git checkout scripts/lib/render.py`） |

### H. 内置物理/排版校正

| 项 | 内容 |
| --- | --- |
| 现状 | 无；本次靠人眼+经验；`style.py` 已有 `contrast()/ensure_contrast()` 未被复用 |
| 改法 | ① three.js **光照 π 校正**常量 + `intensity_for(target_color)` 反解；② **标题对比度自动闸门**（复用 `style.ensure_contrast`，不达标自动留白/调色）；③ **CJK 字形回退检测**（无头 Chromium 内实测字形可取，缺失即报 `fix_hint`）；④ **双语缩放跳变检测**（中英同行字号差 > 阈值即告警） |
| 省 | **≈5k token / ≈10 min**（避免豆腐块/低对比返工）**[估算]** |
| 验收 | 故意用不存在的中文字体 → 必须报错；对比度 3.1:1 的标题 → 必须自动修正到 ≥4.5:1 |
| 回滚 | 全部为 check 类，失败只告警不阻断（可 `--strict` 升级为阻断） |

---

## 5. P2 — 交付体验

| 项 | 内容 | 省 |
| --- | --- | --- |
| **I 音频** | ① 内置混音目标校验（均值/峰值窗口）与**自动增益**；② **cue 库**：whoosh/tick/impact/riser/pop/chime 参数化生成器（把本次 250 行一次性合成脚本沉淀为 8–12 个生成器）；③ "预混成一条 WAV"配方（绕开 `amix` 按轨数衰减） | **≈5k token / ≈25 min** **[实测]** |
| **J 推广/署名** | ① 主片零文字时**自动生成独立结尾卡**（推荐文案 + star 引导），主片保持零文字；② **可发布交付说明模板**（规格/验收数值/署名/许可） | **≈2k token / ≈5 min** |
| **K 错误提示** | ① scene 未 ready：捕获 `pageerror`/console + 最后成功帧时间，输出 `{code:SCENE_NOT_READY, t_last_ok, errors[]}`，替代 "never signalled ready"；② setup 失败（网络/key/模型）结构化；③ ffmpeg 失败给"哪个 filter/哪个输入"定位 | **≈3k token / ≈10 min** |

### P2 增补（来自实战复盘，非原始清单）

| 项 | 内容 | 收益 |
| --- | --- | --- |
| **L 快车道** | 文档承认一种模式：**用户已给出秒级时间轴 + 文字白名单 + 负面清单 = 已批准计划**，可直接开工，但必须在输出里显式声明"我把 brief 当作已批准计划" | 省一轮往返（本次若照字面执行会先问 5 个问题） |
| **M 体裁档位** | taste 闸门加 `register`：`ui-concept` / `data-viz` / `narrative`。UI 概念片里"窗口/面板/假外壳"是**题材本身**，不该被 A7 闸门拦 | 避免 agent 与 brief 打架 |
| **N 英文摘要** | 只翻最常读的 3 篇中文（choreography 320 / taste 66 / rhythm-handoff 143 = **529 行**），其余保持中文 | **≈8k token/次**（英文 agent 读中文的额外成本） |
| **O 交付尺寸文档** | `pipeline.md` 增补：4K 实测 ms/帧、内存上限、`preset`/重启/GPU 旗标、并发与内存的关系 | 消除 §3.2 #5/#6 |

---

## 6. 汇总（估算）

| 批次 | token 节省 | 墙钟节省 | 备注 |
| --- | --- | --- | --- |
| P0（A–D） | ≈47k | ≈40 min | 130–150k → 85–100k；85 → 45 min |
| P1（E–H） | ≈33k | 渲染 −70~83%，另有 ≈30 min | E 是最痛单项，G 是最大时间收益 |
| P2（I–K+L–O） | ≈18k | ≈40 min | 音频与英文摘要占大头 |
| **合计** | **≈98k** | **≈110 min** | 同项目重跑：**token −60~70%、墙钟 −70%**，均超过 −40% / −50% 目标 |

保守起见按"只做 P0+P1"汇报：**token −55%、总墙钟 −60%、渲染 −70%**。

---

## 7. 批次顺序与回滚点

| 批次 | 内容 | 触碰既有文件 | 回滚方式 |
| --- | --- | --- | --- |
| **B0** | `AUDIT.md` + `PLAN.md`（本批，纯文档） | 无 | 删文档 |
| **B1** | P0-B 结构化输出/错误 | `scripts/vs.py`（调用点） | revert vs.py + 删 `fmt.py` |
| **B2** | P0-A 接口自描述 | 各脚本 argparse 文案 | 删生成物 |
| **B3** | P0-C 预览双通道 | `render_segment.mjs`（新增 `--probe` 分支） | 删分支 |
| **B4** | P0-D 读取缓存 | `vs.py` 新增子命令 | 删子命令 |
| **— 验收 P0 —** | 跑 §1 前 5 条 | | |
| **B5** | P1-E patch 工具 | 无（纯新增） | 删脚本 |
| **B6** | P1-F scene_check | `vs.py render` 前调用（可 `--no-check`） | 关开关 |
| **B7** | P1-G 渲染加速 | `scripts/lib/render.py` | `git checkout render.py` |
| **B8** | P1-H 校正 | 新增 check 模块 | 删模块 |
| **— 验收 P1 —** | 跑 §1 第 6~8 条 | | |
| **B9** | P2 I/J/K | 新增模块 + 文案 | 逐个删 |
| **B10** | 文档整合（SKILL 精简 + 英文摘要 + O 项） | `SKILL.md` / `pipeline.md` | revert 文档 |

每批结束提交一份**实测/估算节省**（token 用 §1 口径，时间用打点）。

---

## 8. 非目标（明确不做）

1. 不改 `seek(t)` 契约、不改场景写法、不引入第二套渲染路径。
2. 不把交付物渲两遍：切分片 = **一次导出的并行实现**；1/4 分辨率是**预检**不是交付渲染。
3. 不动 vendored 库与 `assets/lib`。
4. 不做 GUI、不做云渲染、不引入 npm 依赖（保持零外部运行时依赖）。
5. 不改已有命令的默认语义（`--json/--verbose/--slices/--report` 全部为显式开关）；
   唯一默认变化是**日志默认变摘要**（用户明确要求），`--verbose` 完全恢复。

## 9. 需要你确认的三点

1. **验收口径**：token 用"工具输出字节 + 被读文件字节 + 回合数"作代理指标（模型自身推理 token 无法自测），是否接受？
2. **确定性标准**：默认保证**帧哈希一致**、比特一致需 `--deterministic-encode`（x264 多线程会改变码流字节），是否接受这一修订？
3. **批次顺序**：按 B1→B10 顺序，还是先做你最在意的 P1-G（渲染加速）？我建议先 B1/B3/B5（工具类，风险最低、收益最直接），再进 B7。
